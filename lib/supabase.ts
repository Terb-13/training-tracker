/**
 * Supabase entry — use browser client on the client, server helper in RSC / actions.
 */
export { createClient } from "./supabase/client";
export { createServerSupabaseClient } from "./supabase/server";
